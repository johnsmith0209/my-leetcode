/**
 * @param {number} num
 * @return {number}
 */
var addDigits = function(num) {
    var rzt = num % 9;
    return num === 0 ? 0 : (rzt === 0 ? 9 : rzt);
    // Find out this solution after bunch of times of staring into the equations.
    // But still don't know the reason here.  
};
